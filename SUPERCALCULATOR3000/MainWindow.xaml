<Window x:Class="CalcApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calculator"
        Height="600" Width="420"
        Background="#0F111A"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <Style TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#22263A"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Height" Value="70"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#00E5FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.92" ScaleY="0.92"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Background="#1C1F2A"
                CornerRadius="20"
                Padding="15">

            <StackPanel>

                <TextBlock x:Name="ExpressionText"
                           Foreground="#888"
                           FontSize="16"
                           HorizontalAlignment="Right"/>

                <TextBlock x:Name="Display"
                           Foreground="White"
                           FontSize="40"
                           HorizontalAlignment="Right"
                           Text="0"
                           TextWrapping="Wrap"
                           MaxWidth="360"/>
            </StackPanel>

        </Border>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,20,0,0">

            <Button Content="C" Click="Clear"/>
            <Button Content="(" Click="Btn"/>
            <Button Content=")" Click="Btn"/>
            <Button Content="/" Click="Btn"/>

            <Button Content="7" Click="Btn"/>
            <Button Content="8" Click="Btn"/>
            <Button Content="9" Click="Btn"/>
            <Button Content="*" Click="Btn"/>

            <Button Content="4" Click="Btn"/>
            <Button Content="5" Click="Btn"/>
            <Button Content="6" Click="Btn"/>
            <Button Content="-" Click="Btn"/>

            <Button Content="1" Click="Btn"/>
            <Button Content="2" Click="Btn"/>
            <Button Content="3" Click="Btn"/>
            <Button Content="+" Click="Btn"/>

            <Button Content="0" Click="Btn"/>
            <Button Content="." Click="Btn"/>
            <Button Content="=" Click="Eq"/>
            <Button Content="√" Click="Func"/>

            <Button Content="sin" Click="Func"/>
            <Button Content="cos" Click="Func"/>
            <Button Content="tg" Click="Func"/>
            <Button Content="log" Click="Func"/>

        </UniformGrid>

    </Grid>
</Window>